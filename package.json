{
  "name": "cc2js",
  "packageManager": "bun@1.4.2",
  "version": "1.2.1",
  "description": "CLI that converts Bun-compiled binaries of any Claude Code version into a pure JavaScript build that runs on plain Node 18+",
  "keywords": [
    "claude-code",
    "claude",
    "anthropic",
    "bun",
    "node",
    "compatibility",
    "extract",
    "transpile",
    "cli",
    "npm"
  ],
  "homepage": "https://github.com/cc-friend/cc2js#readme",
  "bugs": "https://github.com/cc-friend/cc2js/issues",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cc-friend/cc2js.git"
  },
  "license": "MIT",
  "author": "Tom Chen (tomchen.org)",
  "bin": {
    "cc2js": "dist/cli.js"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "engines": {
    "node": ">=18"
  },
  "files": [
    "dist",
    "assets",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "clean": "node -e \"require('node:fs').rmSync('dist',{recursive:true,force:true})\"",
    "build": "bun run clean && tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "lint": "biome lint src test",
    "format": "biome format src test",
    "test": "tsx test/index.test.ts",
    "checkall": "bun run typecheck && bun run lint && bun run format && bun run test",
    "fixall": "biome check --write src test",
    "e2e": "tsx test/e2e.ts",
    "release:patch": "vbt patch",
    "release:minor": "vbt minor",
    "release:major": "vbt major",
    "prepublishOnly": "bun run build"
  },
  "vbt": {
    "push": true,
    "preBump": "bun run checkall",
    "postVerRepl": "bun install --lockfile-only --ignore-scripts",
    "commitFiles": [
      "bun.lock"
    ]
  },
  "dependencies": {
    "esbuild-wasm": "^0.28.1",
    "unbunjs": "^2.0.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.2",
    "@types/node": "^26.1.0",
    "tsx": "^4.23.0",
    "typescript": "^6.0.3",
    "vbt": "^2.0.0"
  }
}
